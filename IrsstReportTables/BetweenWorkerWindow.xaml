<Window x:Class="IrsstReportTables.BetweenWorkerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IrsstReportTables"
        mc:Ignorable="d"
        Title="WebExpo C# Examples - IRSST Report" Height="850" Width="1050">
    <Grid>
        <TextBlock FontSize="12" FontWeight="Bold" TextAlignment="Center" HorizontalAlignment="Left" Margin="40,10,0,0" TextWrapping="Wrap" Text="Table 6 - Exposure metrics point estimates and credible intervals for an example of Bayesian calculation for the lognormal model (between-worker difference analyses)" VerticalAlignment="Top" Height="22" Width="958" />
        <local:Table6Grid Margin="55,32,55,518.6">
            <DataGrid.Columns>
                <DataGridTextColumn Header = "Parameter" Binding = "{Binding Title}" />
                <DataGridTextColumn Header = "Low within-worker correlation (rho=0.06)" Binding = "{Binding Datum}" />
                <DataGridTextColumn Header = "High within-worker correlation (rho=0.66)" Binding = "{Binding Datum2}" />
            </DataGrid.Columns>
        </local:Table6Grid>

        <TextBlock FontSize="11" FontWeight="Bold" TextAlignment="Center" HorizontalAlignment="Left" Margin="48,308,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="18" Width="950" ><Run Text="Table "/><Run Text="7"/><Run Text=" - "/><Run Text="Worker-specific exposure metrics point "/><Run Text="estimates and "/><Run Text="credible intervals for the least and most exposed workers in two samples with low and high within-worker correlation"/><Run Text=")"/></TextBlock>
        <local:Table7Grid Margin="35,331,35,350">
            <DataGrid.Columns>
                <DataGridTextColumn Header = "Parameter" Binding = "{Binding Title}" Width="Auto" />
                <DataGridTextColumn Header = "Least-exposed worker (rho=0.06)" Binding = "{Binding Datum}" />
                <DataGridTextColumn Header = "Most-exposed worker (rho=0.06)" Binding = "{Binding Datum2}" />
                <DataGridTextColumn Header = "Least-exposed worker (rho=0.66)" Binding = "{Binding Datum3}" />
                <DataGridTextColumn Header = "Most-exposed worker (rho=0.66)" Binding = "{Binding Datum4}" />
            </DataGrid.Columns>
        </local:Table7Grid>

        <TextBlock FontSize="11" FontWeight="Bold" TextAlignment="Center" HorizontalAlignment="Left" Margin="10,490 ,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="17" Width="1009" ><Run Text="Table "/><Run Text="8"/><Run Text=" - "/><Run Text="Exposure metrics point estimates and credible intervals for an example of Bayesian calculation for the lognormal model (between-worker difference analyses) with realistic sample size"/></TextBlock>
        <local:Table8Grid Margin="208,510,200,40">
            <DataGrid.Columns>
                <DataGridTextColumn Header = "Parameter" Binding = "{Binding Title}" Width="Auto" />
                <DataGridTextColumn Header = "Point estimate and 90% credible interval" Binding = "{Binding Datum}" Width="Auto" />
            </DataGrid.Columns>
        </local:Table8Grid>


    </Grid>
</Window>
